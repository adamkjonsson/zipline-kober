# API reference

Generated from the source. Everything is re-exported at the package top level,
so `import kober` reaches all of it; each symbol is documented below under the
module that defines it.

The reading order that makes sense is the pipeline's, which is how this list is
ordered rather than alphabetically — see [Architecture](../dev/architecture.md).

```{toctree}
:maxdepth: 1

spec
expr
loader
check
node
cursor
decoder
emit
stage
ops
pygen
runtime
errors
cli
```
